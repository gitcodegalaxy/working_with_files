# Открыть текст для чтения 
f = open('text.txt', 'r')

f.read() - считывает содержимое файла в строку
f.readline() -  считывает файл построчно
f.readlines() - считывает строки файла и создает список из строк

'r'	открытие на чтение (является значением по умолчанию).
'w'	открытие на запись, содержимое файла удаляется, если файла не существует, создается новый.
'x'	открытие на запись, если файла не существует, иначе исключение.
'a'	открытие на дозапись, информация добавляется в конец файла.
'b'	открытие в двоичном режиме.
't'	открытие в текстовом режиме (является значением по умолчанию).
'+'	открытие на чтение и запись


# Кодировка для работы с русским текстом на Винде. Правильно указать кодировку
with open('test.txt', 'r', encoding='utf-8') as f:
    print("utf-8")
    text = f.read()
    print("Done")
# Неправильно
with open('test.txt', 'r') as f:
    print("default")
    text = f.read()
    print("Done")
    
# нужно обязательно закрыть 
f.close()




# 2 СПОСОБ

with open('example.txt') as f:
    # работа с файлом

# Закрывать не надо



#  3 СПОСОБ

f = open('example.txt','r')
try:
   # работа с файлом
finally:
   f.close()
    
